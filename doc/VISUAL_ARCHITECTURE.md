# 🎨 Visual Architecture Guide

## Deployment Scenarios

### Scenario 1: Deploy Semua Services (Development)

```
┌─────────────────────────────────────────────────────────┐
│  YOU RUN: ./deploy-dev.sh                               │
└─────────────────────────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────┐
│  DOCKER COMPOSE STARTS:                                 │
│                                                          │
│  1. MariaDB (with platform.sql + restore.sql)           │
│  2. Redis                                               │
│  3. MinIO                                               │
│  4. Cosmic Media Streaming (Laravel)                    │
│     - App (port 8000)                                   │
│     - Queue Worker                                      │
│     - Scheduler                                         │
│  5. Generate PDF (Node.js, port 3333)                   │
│  6. Remote Android (Python, port 3001)                  │
│  7. phpMyAdmin (port 8080)                              │
│  8. Redis Commander (port 8081)                         │
└─────────────────────────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────┐
│  ACCESS SERVICES:                                       │
│  http://localhost:8000  - Cosmic Media (Main App)      │
│  http://localhost:3333  - Generate PDF                 │
│  http://localhost:3001  - Remote Android               │
│  http://localhost:8080  - phpMyAdmin                   │
│  http://localhost:8081  - Redis Commander              │
│  http://localhost:9001  - MinIO Console                │
└─────────────────────────────────────────────────────────┘
```

### Scenario 2: Deploy Cosmic Media Saja (Development)

```
┌─────────────────────────────────────────────────────────┐
│  YOU RUN:                                               │
│  cd cosmic-media-streaming-dpr                          │
│  ./deploy-dev.sh                                        │
└─────────────────────────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────┐
│  DOCKER COMPOSE STARTS:                                 │
│                                                          │
│  1. MariaDB (standalone)                                │
│  2. Redis (standalone)                                  │
│  3. MinIO (standalone)                                  │
│  4. Cosmic Media Streaming Only                         │
│     - App (port 8000)                                   │
│     - Queue Worker                                      │
│     - Scheduler                                         │
│  5. phpMyAdmin (port 8080)                              │
│  6. Redis Commander (port 8081)                         │
└─────────────────────────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────┐
│  Note: Generate PDF & Remote Android                    │
│        perlu dijalankan terpisah jika diperlukan        │
└─────────────────────────────────────────────────────────┘
```

---

## Network Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        Internet / Users                          │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │  Nginx Reverse Proxy │
                    │    (Port 80/443)     │
                    └──────────────────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼
┌───────────────┐      ┌──────────────┐      ┌──────────────┐
│ Cosmic Media  │      │ Generate PDF │      │    Remote    │
│   :8000       │◄────►│    :3333     │      │   Android    │
│               │      │   :3335(WS)  │      │    :3001     │
└───────────────┘      └──────────────┘      └──────────────┘
        │                      │                      │
        │                      │                      │
        └──────────────────────┼──────────────────────┘
                               │
                    Docker Network: kiosk-net
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼
┌───────────────┐      ┌──────────────┐      ┌──────────────┐
│   MariaDB     │      │    Redis     │      │    MinIO     │
│    :3306      │      │    :6379     │      │ :9000/:9001  │
│  (Database)   │      │ (Cache/Queue)│      │   (Storage)  │
└───────────────┘      └──────────────┘      └──────────────┘
```

---

## Data Flow Diagrams

### 1. User Request Flow

```
┌──────────┐
│  User    │
│ Browser  │
└────┬─────┘
     │ HTTP Request
     ▼
┌─────────────────┐
│  Nginx          │  - Route to service
│  Reverse Proxy  │  - Load balancing
└────┬────────────┘  - Rate limiting
     │
     ▼
┌─────────────────┐
│ Cosmic Media    │  - Process request
│ Streaming       │  - Business logic
└────┬────────────┘  - Return response
     │
     ├─────────────────────┐
     │                     │
     ▼                     ▼
┌─────────────┐    ┌──────────────┐
│  MariaDB    │    │    Redis     │
│  (Data)     │    │   (Cache)    │
└─────────────┘    └──────────────┘
     │
     │ Response
     ▼
┌──────────┐
│  User    │
│ Browser  │
└──────────┘
```

### 2. PDF Generation Flow

```
┌─────────────────┐
│ Cosmic Media    │  1. Request PDF
│ Streaming       ├──────────────────┐
└─────────────────┘                  │
                                     │ HTTP POST
                                     ▼
                            ┌────────────────┐
                            │  Generate PDF  │
                            │    Service     │
                            └────────┬───────┘
                                     │
                    ┌────────────────┼────────────────┐
                    │                │                │
                    ▼                ▼                ▼
            ┌──────────────┐ ┌──────────┐  ┌──────────────┐
            │   MariaDB    │ │ MinIO    │  │  Puppeteer   │
            │ (Get Layout) │ │ (Upload) │  │ (Render PDF) │
            └──────────────┘ └──────────┘  └──────────────┘
                    │                │                │
                    └────────────────┼────────────────┘
                                     │
                                     ▼
                            ┌────────────────┐
                            │  PDF File URL  │
                            └────────┬───────┘
                                     │
                                     │ Return URL
                                     ▼
                            ┌─────────────────┐
                            │ Cosmic Media    │
                            │ Streaming       │
                            └─────────────────┘
```

### 3. Queue Processing Flow

```
┌─────────────────┐
│ Cosmic Media    │  1. Dispatch Job
│ (Web)           ├──────────────┐
└─────────────────┘              │
                                 ▼
                         ┌───────────────┐
                         │     Redis     │
                         │  Job Queue    │
                         └───────┬───────┘
                                 │
                                 │ 2. Pick Job
                                 ▼
                         ┌───────────────┐
                         │ Queue Worker  │
                         │   (Laravel)   │
                         └───────┬───────┘
                                 │
                ┌────────────────┼────────────────┐
                │                │                │
                ▼                ▼                ▼
        ┌──────────────┐ ┌──────────┐  ┌──────────────┐
        │   MariaDB    │ │ MinIO    │  │  External    │
        │ (Update DB)  │ │ (Upload) │  │   Services   │
        └──────────────┘ └──────────┘  └──────────────┘
```

### 4. Real-time Update Flow (WebSocket)

```
┌──────────────┐                         ┌──────────────┐
│  Kiosk       │                         │  Admin       │
│  Device      │                         │  Dashboard   │
└──────┬───────┘                         └──────┬───────┘
       │                                        │
       │ WebSocket Connection                   │
       └────────────────┬───────────────────────┘
                        │
                        ▼
              ┌────────────────────┐
              │  Generate PDF      │
              │  WebSocket Server  │
              │     :3335          │
              └─────────┬──────────┘
                        │
                        │ Broadcast Updates
                        │
       ┌────────────────┼────────────────┐
       │                                 │
       ▼                                 ▼
┌──────────────┐                  ┌──────────────┐
│  Kiosk       │                  │  Admin       │
│  Device      │                  │  Dashboard   │
│ (Auto Update)│                  │ (Live View)  │
└──────────────┘                  └──────────────┘
```

---

## Service Interactions Matrix

```
┌─────────────────┬─────────┬───────┬────────┬─────────┬─────────────┐
│                 │ MariaDB │ Redis │ MinIO  │Gen. PDF │ Remote Andr.│
├─────────────────┼─────────┼───────┼────────┼─────────┼─────────────┤
│ Cosmic Media    │   ✓✓✓   │  ✓✓✓  │  ✓✓✓   │   ✓✓    │     ✓✓      │
│ Generate PDF    │    ✓    │   -   │   ✓    │    -    │      -      │
│ Remote Android  │   ✓✓    │   -   │   -    │    -    │      -      │
└─────────────────┴─────────┴───────┴────────┴─────────┴─────────────┘

Legend:
✓✓✓ = Heavy usage (read/write frequently)
✓✓  = Moderate usage (occasional API calls)
✓   = Light usage (read-only or rare updates)
-   = No interaction
```

---

## Deployment Comparison

### Development vs Production

```
┌─────────────────────────────────────────────────────────────────┐
│                      DEVELOPMENT                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                      │
│  │ Cosmic   │  │Generate  │  │ Remote   │                      │
│  │ Media    │  │  PDF     │  │ Android  │                      │
│  └──────────┘  └──────────┘  └──────────┘                      │
│       │             │              │                            │
│       └─────────────┼──────────────┘                            │
│                     │                                           │
│  ┌──────────┐  ┌───┴─────┐  ┌──────────┐                      │
│  │ MariaDB  │  │  Redis  │  │  MinIO   │                      │
│  └──────────┘  └─────────┘  └──────────┘                      │
│                                                                  │
│  Development Tools:                                             │
│  - phpMyAdmin (DB management)                                   │
│  - Redis Commander (Queue monitoring)                           │
│  - Hot reload enabled                                           │
│  - Debug mode ON                                                │
│  - Verbose logging                                              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      PRODUCTION                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────┐                     │
│  │         Nginx Reverse Proxy            │                     │
│  │    - SSL/TLS termination               │                     │
│  │    - Load balancing                    │                     │
│  │    - Rate limiting                     │                     │
│  └────────────────┬───────────────────────┘                     │
│                   │                                             │
│  ┌────────────────┼───────────────────┐                        │
│  │                │                   │                        │
│  ▼                ▼                   ▼                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                     │
│  │ Cosmic   │  │Generate  │  │ Remote   │                     │
│  │ Media x3 │  │  PDF     │  │ Android  │                     │
│  └──────────┘  └──────────┘  └──────────┘                     │
│  ┌──────────┐                                                   │
│  │ Queue x2 │                                                   │
│  └──────────┘                                                   │
│       │             │              │                            │
│       └─────────────┼──────────────┘                            │
│                     │                                           │
│  ┌──────────┐  ┌───┴─────┐  ┌──────────┐                      │
│  │ MariaDB  │  │  Redis  │  │  MinIO   │                      │
│  │(Optimized│  │ (2GB)   │  │(Replicas)│                      │
│  └──────────┘  └─────────┘  └──────────┘                      │
│                                                                  │
│  Production Features:                                           │
│  - Multiple workers (scalability)                               │
│  - Health checks enabled                                        │
│  - Logging to files                                             │
│  - Debug mode OFF                                               │
│  - Caching optimized                                            │
│  - Monitoring ready                                             │
│  - Backup automated                                             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## File Organization Visual

```
/home/ubuntu/kiosk/
│
├── 📋 DOCUMENTATION (Read these first!)
│   ├── README.md                      ← Start here! Complete guide
│   ├── QUICK_START.md                 ← 5-minute quick reference
│   ├── SUMMARY.md                     ← What was created & why
│   ├── MICROSERVICES_READINESS_ANALYSIS.md ← Architecture details
│   ├── DEPLOYMENT_CHECKLIST.md        ← Pre/post deployment tasks
│   └── VISUAL_ARCHITECTURE.md         ← This file (diagrams)
│
├── 🚀 RUN THESE TO DEPLOY
│   ├── deploy-dev.sh                  ← Deploy ALL services (dev)
│   └── deploy-prod.sh                 ← Deploy ALL services (prod)
│
├── 🐳 DOCKER CONFIGURATIONS
│   ├── docker-compose.dev.yml         ← All services (development)
│   └── docker-compose.prod.yml        ← All services (production)
│
├── ⚙️ CONFIGURATIONS
│   ├── .env.example                   ← Copy to .env & customize
│   ├── platform.sql                   ← Database dump (auto-import)
│   ├── restore.sql                    ← DB restoration script
│   └── nginx/
│       └── nginx.conf                 ← Reverse proxy config
│
└── 📦 MICROSERVICES
    │
    ├── cosmic-media-streaming-dpr/    ← SERVICE #1 (Laravel CMS)
    │   ├── deploy-dev.sh              ← Deploy this service only (dev)
    │   ├── deploy.sh                  ← Deploy this service only (prod)
    │   ├── docker-compose.dev.yml     ← Standalone dev config
    │   ├── docker-compose.yml         ← Standalone prod config
    │   └── Dockerfile.dev             ← Development Docker image
    │
    ├── generate-pdf/                  ← SERVICE #2 (Node.js PDF)
    │   ├── docker-compose.yml         ← Service config
    │   └── Dockerfile                 ← Docker image
    │
    └── remote-android-device/         ← SERVICE #3 (Python Device)
        ├── docker-compose.yml         ← Service config
        └── Dockerfile                 ← Docker image
```

---

## Decision Tree: Which Command to Run?

```
                    START
                      │
                      ▼
        ┌─────────────────────────────┐
        │ What do you want to deploy? │
        └─────────────┬───────────────┘
                      │
        ┌─────────────┼─────────────┐
        │             │             │
        ▼             ▼             ▼
    ┌───────┐   ┌─────────┐   ┌─────────────┐
    │ ALL   │   │ Cosmic  │   │ Individual  │
    │ 3     │   │ Media   │   │ Other       │
    │Services│   │ Only    │   │ Service     │
    └───┬───┘   └────┬────┘   └──────┬──────┘
        │            │                │
        ▼            ▼                ▼
    ┌────────┐   ┌────────┐      ┌──────────┐
    │ Dev or │   │ Dev or │      │ Use their│
    │ Prod?  │   │ Prod?  │      │ existing │
    └───┬────┘   └───┬────┘      │ docker   │
        │            │            │ compose  │
    ┌───┴───┐    ┌───┴───┐       └──────────┘
    │       │    │       │
    ▼       ▼    ▼       ▼
┌──────┐ ┌───┐ ┌──┐  ┌───┐
│ Dev  │ │Pro│ │Dev│  │Pro│
└──┬───┘ └─┬─┘ └─┬┘  └─┬─┘
   │       │     │     │
   ▼       ▼     ▼     ▼
./deploy-dev.sh │ cd cosmic-media-streaming-dpr/
  (ROOT)   │    │ ./deploy-dev.sh
           │    │        │
./deploy-prod.sh│        │
  (ROOT)        │        ./deploy.sh
                │
                ./deploy-prod.sh
```

---

## Port Mapping Visual

```
┌─────────────────────────────────────────────────────────┐
│                    HOST MACHINE                          │
│                    (Your Server)                         │
└─────────────────────────────────────────────────────────┘
           │         │         │         │
           │ :8000   │ :3333   │ :3001   │ :8080
           │         │         │         │
           ▼         ▼         ▼         ▼
┌──────────┴─────────┴─────────┴─────────┴───────────────┐
│                  DOCKER NETWORK                          │
│                    (kiosk-net)                           │
│                                                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │   Cosmic    │  │  Generate   │  │   Remote    │    │
│  │   Media     │  │    PDF      │  │   Android   │    │
│  │   :8000     │  │   :3333     │  │   :3001     │    │
│  └─────────────┘  └─────────────┘  └─────────────┘    │
│                                                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │  phpMyAdmin │  │    Redis    │  │    MinIO    │    │
│  │   :8080     │  │  Commander  │  │   Console   │    │
│  │             │  │   :8081     │  │   :9001     │    │
│  └─────────────┘  └─────────────┘  └─────────────┘    │
│                                                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │   MariaDB   │  │    Redis    │  │    MinIO    │    │
│  │   :3306     │  │    :6379    │  │    :9000    │    │
│  └─────────────┘  └─────────────┘  └─────────────┘    │
└──────────────────────────────────────────────────────────┘

ACCESSIBLE FROM HOST:
✓ http://localhost:8000  - Cosmic Media
✓ http://localhost:3333  - Generate PDF
✓ http://localhost:3001  - Remote Android
✓ http://localhost:8080  - phpMyAdmin
✓ http://localhost:8081  - Redis Commander
✓ http://localhost:9001  - MinIO Console
✓ localhost:3306         - MariaDB
✓ localhost:6379         - Redis
```

---

**Visual Guide Version:** 1.0.0  
**Last Updated:** January 22, 2026
